"use client";
import { useEffect, useState } from "react";
import { Download, FileCheck2, Loader2, Upload } from "lucide-react";
import { AuthGate, getAccessToken } from "../../../components/auth-provider";
const API=process.env.NEXT_PUBLIC_API_URL??"http://localhost:4000/api/v1";type Exam={id:string;name:string;examDate:string;maximumMarks:number;subject:{name:string};questionPaper:{fileName:string}|null;answerSheet:{fileName:string;status:string;isLate:boolean;marksObtained:string|null;teacherRemarks:string|null}|null};const headers=()=>({"Content-Type":"application/json",Authorization:`Bearer ${getAccessToken()??""}`});async function parse(response:Response){const value=await response.json().catch(()=>null);if(!response.ok)throw Error(value?.error?.message??"Request failed");return value}async function encoded(file:File){return new Promise<string>((resolve,reject)=>{const reader=new FileReader();reader.onerror=()=>reject(reader.error);reader.onload=()=>resolve(String(reader.result).split(",")[1]??"");reader.readAsDataURL(file)})}
function Content(){const[rows,setRows]=useState<Exam[]>([]),[loading,setLoading]=useState(true),[error,setError]=useState(""),[notice,setNotice]=useState("");async function load(){setLoading(true);try{setRows((await fetch(`${API}/exam-workflow/examinations`,{headers:headers()}).then(parse)).data);setError("")}catch(cause){setError(cause instanceof Error?cause.message:"Unable to load examinations")}finally{setLoading(false)}}useEffect(()=>{void load()},[]);async function submit(exam:Exam,file:File|null){if(!file)return;try{await fetch(`${API}/exam-workflow/examinations/${exam.id}/answer-sheet`,{method:"PUT",headers:headers(),body:JSON.stringify({fileName:file.name,mimeType:file.type,base64:await encoded(file)})}).then(parse);setNotice("Answer sheet submitted successfully");await load()}catch(cause){setError(cause instanceof Error?cause.message:"Submission failed")}}async function paper(exam:Exam){const response=await fetch(`${API}/exam-workflow/examinations/${exam.id}/question-paper`,{headers:{Authorization:`Bearer ${getAccessToken()??""}`}});if(!response.ok)return setError((await response.json()).error?.message??"Paper unavailable");const url=URL.createObjectURL(await response.blob());window.open(url,"_blank");setTimeout(()=>URL.revokeObjectURL(url),60000)}return <main className="min-h-screen bg-slate-50 p-5 md:p-10"><div className="mx-auto max-w-5xl"><header><p className="text-sm font-bold text-brand-700">STUDENT PORTAL</p><h1 className="text-3xl font-bold">Examinations & Answer Sheets</h1><p className="text-slate-500">Download published papers and submit your own answer sheet securely.</p></header>{notice&&<p className="mt-4 rounded-xl bg-emerald-50 p-3 text-emerald-700">{notice}</p>}{error&&<p role="alert" className="mt-4 rounded-xl bg-red-50 p-3 text-red-700">{error}</p>}<section className="mt-6 space-y-4">{loading?<Loader2 className="mx-auto mt-20 animate-spin"/>:rows.length?rows.map(exam=><article key={exam.id} className="rounded-2xl border bg-white p-5"><div className="flex flex-wrap items-start justify-between gap-3"><div><h2 className="text-xl font-bold">{exam.name}</h2><p className="text-sm text-slate-500">{exam.subject.name} · {exam.examDate.slice(0,10)} · {exam.maximumMarks} marks</p></div><div className="flex gap-2">{exam.questionPaper&&<button className="btn" onClick={()=>void paper(exam)}><Download size={16}/>Question Paper</button>}<label className="btn cursor-pointer bg-brand-700 text-white"><Upload size={16}/>{exam.answerSheet?"Replace Answer Sheet":"Submit Answer Sheet"}<input className="hidden" type="file" accept=".pdf,image/png,image/jpeg" onChange={e=>void submit(exam,e.target.files?.[0]??null)}/></label></div></div>{exam.answerSheet&&<div className="mt-4 rounded-xl bg-slate-50 p-3 text-sm"><b>{exam.answerSheet.status}</b>{exam.answerSheet.isLate&&<span className="text-amber-700"> · Late submission</span>}<p>{exam.answerSheet.fileName}</p>{exam.answerSheet.marksObtained!==null&&<p className="mt-2">Marks: <b>{exam.answerSheet.marksObtained}/{exam.maximumMarks}</b></p>}{exam.answerSheet.teacherRemarks&&<p>Feedback: {exam.answerSheet.teacherRemarks}</p>}</div>}</article>):<div className="py-20 text-center text-slate-500"><FileCheck2 className="mx-auto mb-2"/>No examinations are currently available.</div>}</section></div><style jsx global>{`.btn{display:inline-flex;align-items:center;justify-content:center;gap:.4rem;border:1px solid #dbe1ea;border-radius:.75rem;padding:.65rem .9rem;font-size:.875rem;font-weight:700}`}</style></main>}
export default function Page(){return <AuthGate roles={["STUDENT"]}><Content/></AuthGate>}
