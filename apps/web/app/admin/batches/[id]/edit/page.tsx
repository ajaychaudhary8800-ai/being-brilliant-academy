"use client";import{useParams}from"next/navigation";import{useEffect,useState}from"react";import BatchForm from"../../../../../components/batch-form";import{AuthGate,getAccessToken}from"../../../../../components/auth-provider";import Sidebar from"../../../../../components/sidebar";const api=process.env.NEXT_PUBLIC_API_URL??"http://localhost:4000/api/v1";type Batch={id:string;name:string;code:string;academicSession:string;startsAt:string;endsAt:string|null;capacity:number;timing:string;days:string[];classroom:string|null;feesPaise:number;status:string;remarks:string|null;branch:{id:string};course:{id:string}|null;teacher:{id:string}|null};function Content(){const{id}=useParams<{id:string}>(),[batch,setBatch]=useState<Batch|null>(null),[error,setError]=useState("");useEffect(()=>{fetch(`${api}/admin/batches/${id}`,{headers:{Authorization:`Bearer ${getAccessToken()??""}`}}).then(async r=>{const j=await r.json();if(!r.ok)throw Error(j?.error?.message??"Unable to load batch");setBatch(j.data)}).catch(e=>setError(e instanceof Error?e.message:"Unable to load batch"))},[id]);return <div className="min-h-screen bg-slate-50 dark:bg-slate-950"><Sidebar/><main className="p-5 md:ml-64 md:p-10"><div className="mx-auto max-w-5xl"><h1 className="text-3xl font-bold">Edit Batch</h1>{error?<p className="mt-5 rounded-xl bg-red-50 p-4 text-red-700">{error}</p>:batch?<BatchForm batch={batch}/>:<p className="mt-6">Loading batch…</p>}</div></main></div>}export default function Page(){return <AuthGate roles={["SUPER_ADMIN","BRANCH_ADMIN"]}><Content/></AuthGate>}
