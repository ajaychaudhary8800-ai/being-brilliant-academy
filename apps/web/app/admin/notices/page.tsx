"use client";import{FormEvent,useCallback,useEffect,useState}from"react";import{Archive,Loader2,Plus,Search,X}from"lucide-react";import{AuthGate,getAccessToken}from"../../../components/auth-provider";import Sidebar from"../../../components/sidebar";const API=process.env.NEXT_PUBLIC_API_URL??"http://localhost:4000/api/v1",headers=()=>({"Content-Type":"application/json",Authorization:`Bearer ${getAccessToken()??""}`}),empty={title:"",body:"",category:"GENERAL",priority:"NORMAL",audience:"",publishedAt:new Date().toISOString().slice(0,16),expiresAt:"",isPinned:false,requiresAcknowledgement:false};type Notice={id:string;title:string;body:string;category:string|null;priority:string;isPinned:boolean;requiresAcknowledgement:boolean;publishedAt:string;_count:{reads:number}};async function json(r:Response){const b=await r.json().catch(()=>null);if(!r.ok)throw Error(b?.error?.message??"Request failed");return b}function Content(){const[rows,setRows]=useState<Notice[]>([]),[form,setForm]=useState<any>(empty),[show,setShow]=useState(false),[search,setSearch]=useState(""),[loading,setLoading]=useState(true),[error,setError]=useState(""),[notice,setNotice]=useState("");const load=useCallback(async()=>{setLoading(true);try{setRows((await fetch(`${API}/notices?limit=100&search=${encodeURIComponent(search)}`,{headers:headers()}).then(json)).data);setError("")}catch(c){setError(c instanceof Error?c.message:"Load failed")}finally{setLoading(false)}},[search]);useEffect(()=>{void load()},[load]);async function save(e:FormEvent){e.preventDefault();try{await fetch(`${API}/admin/notices`,{method:"POST",headers:headers(),body:JSON.stringify({...form,audience:form.audience||null,publishedAt:new Date(form.publishedAt).toISOString(),expiresAt:form.expiresAt?new Date(form.expiresAt).toISOString():null})}).then(json);setForm(empty);setShow(false);setNotice("Notice published");await load()}catch(c){setError(c instanceof Error?c.message:"Publish failed")}}async function archive(id:string){const r=await fetch(`${API}/admin/notices/${id}`,{method:"DELETE",headers:headers()});if(!r.ok)return setError((await r.json()).error?.message);setNotice("Notice archived");await load()}return <div className="min-h-screen bg-slate-50"><Sidebar/><main className="p-5 md:ml-64 md:p-10"><div className="mx-auto max-w-6xl"><header className="flex justify-between"><div><h1 className="text-3xl font-bold">Notice Board</h1><p className="text-slate-500">Targeted, pinned and acknowledgement-aware notices.</p></div><button className="btn bg-brand-700 text-white" onClick={()=>setShow(true)}><Plus/>New Notice</button></header>{notice&&<p className="mt-4 rounded bg-emerald-50 p-3 text-emerald-700">{notice}</p>}{error&&<p className="mt-4 rounded bg-red-50 p-3 text-red-700">{error}</p>}<label className="relative mt-5 block"><Search className="absolute left-3 top-3" size={16}/><input className="field pl-9" value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search notices"/></label><section className="mt-5 space-y-3">{loading?<Loader2 className="mx-auto animate-spin"/>:rows.map(x=><article key={x.id} className="rounded-2xl border bg-white p-5"><div className="flex justify-between"><div><span className="text-xs font-bold text-brand-700">{x.isPinned?"PINNED · ":""}{x.category} · {x.priority}</span><h2 className="text-xl font-bold">{x.title}</h2></div><button onClick={()=>void archive(x.id)}><Archive/></button></div><p className="mt-2 whitespace-pre-wrap">{x.body}</p><p className="mt-3 text-xs text-slate-500">Published {new Date(x.publishedAt).toLocaleString("en-IN")} · {x._count.reads} read/acknowledged{x.requiresAcknowledgement?" · acknowledgement required":""}</p></article>)}</section></div></main>{show&&<div className="fixed inset-0 z-50 grid place-items-center bg-black/50 p-4"><form onSubmit={save} className="grid w-full max-w-2xl gap-3 rounded-2xl bg-white p-6 sm:grid-cols-2"><button type="button" className="justify-self-end sm:col-span-2" onClick={()=>setShow(false)}><X/></button><input required className="field sm:col-span-2" placeholder="Title" value={form.title} onChange={e=>setForm({...form,title:e.target.value})}/><textarea required className="field sm:col-span-2" placeholder="Notice content" value={form.body} onChange={e=>setForm({...form,body:e.target.value})}/><input className="field" placeholder="Category" value={form.category} onChange={e=>setForm({...form,category:e.target.value})}/><select className="field" value={form.priority} onChange={e=>setForm({...form,priority:e.target.value})}>{["LOW","NORMAL","HIGH","URGENT"].map(x=><option key={x}>{x}</option>)}</select><select className="field" value={form.audience} onChange={e=>setForm({...form,audience:e.target.value})}><option value="">Everyone</option>{["STUDENT","PARENT","TEACHER"].map(x=><option key={x}>{x}</option>)}</select><input className="field" type="datetime-local" value={form.publishedAt} onChange={e=>setForm({...form,publishedAt:e.target.value})}/><label><input type="checkbox" checked={form.isPinned} onChange={e=>setForm({...form,isPinned:e.target.checked})}/> Pinned</label><label><input type="checkbox" checked={form.requiresAcknowledgement} onChange={e=>setForm({...form,requiresAcknowledgement:e.target.checked})}/> Require acknowledgement</label><button className="btn bg-brand-700 text-white sm:col-span-2">Publish Notice</button></form></div>}<style jsx global>{`.field{width:100%;border:1px solid #dbe1ea;border-radius:.75rem;padding:.65rem .8rem}.btn{display:inline-flex;align-items:center;justify-content:center;gap:.4rem;border:1px solid #dbe1ea;border-radius:.75rem;padding:.65rem .9rem;font-weight:700}`}</style></div>}export default function Page(){return <AuthGate roles={["SUPER_ADMIN","BRANCH_ADMIN"]}><Content/></AuthGate>}
