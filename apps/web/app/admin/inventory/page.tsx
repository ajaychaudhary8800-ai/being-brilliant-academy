"use client";
import{useCallback,useEffect,useState}from"react";import{AlertTriangle,Boxes,Download,PackageCheck,Search,Store,TicketCheck}from"lucide-react";import{ProtectedAdminWorkspace}from"../../../components/admin-workspace";import{errorMessage,getAccessToken}from"../../../components/auth-provider";const API=process.env.NEXT_PUBLIC_API_URL??"http://localhost:4000/api/v1";type Row=Record<string,unknown>;async function api(path:string){const response=await fetch(`${API}${path}`,{headers:{Authorization:`Bearer ${getAccessToken()}`}}),json=await response.json();if(!response.ok)throw new Error(json?.error?.message);return json}async function download(report:string,format:"pdf"|"excel"){const response=await fetch(`${API}/inventory/reports/${report}?format=${format}`,{headers:{Authorization:`Bearer ${getAccessToken()}`}});if(!response.ok)throw new Error("Export failed");const url=URL.createObjectURL(await response.blob()),link=document.createElement("a");link.href=url;link.download=`inventory-${report}.${format==='pdf'?'pdf':'xls'}`;link.click();URL.revokeObjectURL(url)}export default function Page(){const[tab,setTab]=useState("assets"),[rows,setRows]=useState<Row[]>([]),[dashboard,setDashboard]=useState<Row>({}),[search,setSearch]=useState(""),[error,setError]=useState("");const load=useCallback(async()=>{try{setError("");setDashboard((await api("/inventory/dashboard")).data);const path=tab==="assets"?"/inventory/assets":tab==="items"?"/inventory/items":tab==="maintenance"?"/inventory/maintenance":tab==="alerts"?"/inventory/alerts":`/inventory/reports/${tab}`;const result=await api(`${path}?search=${encodeURIComponent(search)}&page=1&limit=50`);setRows(Array.isArray(result.data)?result.data:[...(result.data?.lowStock??[]),...(result.data?.expiring??[])])}catch(e){setError(errorMessage(e))}},[tab,search]);useEffect(()=>{void load()},[load]);const cards=[[Boxes,"Assets",dashboard.assets],[PackageCheck,"Inventory items",dashboard.items],[Store,"Stores",dashboard.stores],[TicketCheck,"Open maintenance",dashboard.openTickets],[AlertTriangle,"Low stock",dashboard.lowStock]];return <ProtectedAdminWorkspace title="Inventory & Asset Management" description="Enterprise asset register, procurement, stock ledger, consumables, maintenance and ERP integrations."><div className="mt-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-5">{cards.map(([Icon,label,value])=>{const I=Icon as typeof Boxes;return <div className="card p-4" key={String(label)}><I className="text-brand-700"/><p className="mt-3 text-xs uppercase text-slate-400">{String(label)}</p><b>{String(value??0)}</b></div>})}</div><div className="mt-6 flex gap-2 overflow-x-auto">{["assets","items","maintenance","alerts","valuation","depreciation","vendors","purchases","movements"].map(name=><button key={name} onClick={()=>setTab(name)} className={`rounded-full px-4 py-2 text-sm font-semibold ${tab===name?"bg-brand-700 text-white":"border bg-white dark:bg-slate-900"}`}>{name}</button>)}</div>{error&&<p className="mt-4 rounded-lg bg-red-50 p-3 text-red-700">{error}</p>}<div className="my-5 flex gap-3"><div className="relative flex-1"><Search className="absolute left-3 top-3" size={17}/><input value={search} onChange={e=>setSearch(e.target.value)} className="w-full rounded-lg border py-2.5 pl-9 dark:bg-slate-900" placeholder="Search assets, tags, barcode, SKU or tickets"/></div><button onClick={()=>void download(tab,"excel")} className="flex items-center gap-2 rounded-lg border px-4"><Download size={17}/>Excel</button><button onClick={()=>void download(tab,"pdf")} className="flex items-center gap-2 rounded-lg border px-4"><Download size={17}/>PDF</button></div><div className="card divide-y dark:divide-slate-800">{rows.map((row,index)=><div className="grid gap-2 p-4 text-sm sm:grid-cols-5" key={String(row.id??index)}>{Object.entries(row).filter(([,value])=>typeof value!=="object").slice(0,5).map(([key,value])=><span key={key}><small className="block uppercase text-slate-400">{key}</small>{String(value)}</span>)}</div>)}{!rows.length&&<p className="p-6 text-slate-500">No records found.</p>}</div><p className="mt-4 text-xs text-slate-500">Asset tagging, transfers, warranties, depreciation, disposal, procurement approvals, receipts, lot/expiry stock, issues, transfers, reorder alerts, maintenance, AMC, safe deletion and module-linked audit history are available through authenticated APIs.</p></ProtectedAdminWorkspace>}
