"use client";import{FormEvent,useEffect,useState}from"react";import{Palette,Save,School}from"lucide-react";import{ProtectedAdminWorkspace}from"../../../components/admin-workspace";import{errorMessage,getAccessToken}from"../../../components/auth-provider";const API=process.env.NEXT_PUBLIC_API_URL??"http://localhost:4000/api/v1";async function api(init?:RequestInit){const x=await fetch(`${API}/organization/settings`,{...init,headers:{"Content-Type":"application/json",Authorization:`Bearer ${getAccessToken()}`}}),j=await x.json();if(!x.ok)throw new Error(j?.error?.message);return j.data}export default function Page(){const[form,setForm]=useState<any>(null),[message,setMessage]=useState("");useEffect(()=>{void api().then(setForm).catch(e=>setMessage(errorMessage(e)))},[]);async function save(e:FormEvent){e.preventDefault();try{setForm(await api({method:"PATCH",body:JSON.stringify({name:form.name,legalName:form.legalName,phone:form.phone,logoUrl:form.logoUrl||null,primaryColor:form.primaryColor,secondaryColor:form.secondaryColor,timezone:form.timezone,locale:form.locale,currency:form.currency,academicYearStartMonth:Number(form.academicYearStartMonth),settings:form.settings})}));setMessage("Settings saved") }catch(x){setMessage(errorMessage(x))}}if(!form)return <ProtectedAdminWorkspace title="School Settings" description="Loading tenant configuration."><p className="mt-6">{message||"Loading…"}</p></ProtectedAdminWorkspace>;return <ProtectedAdminWorkspace title="School Branding & Settings" description="Tenant-specific identity, localization and academic configuration."><form onSubmit={save} className="card mt-6 grid max-w-3xl gap-4 p-6 md:grid-cols-2">{[["name","School name"],["legalName","Legal name"],["phone","Phone"],["logoUrl","Logo URL"],["timezone","Timezone"],["locale","Locale"],["currency","Currency"],["academicYearStartMonth","Academic year start month"]].map(([key,label])=><label className="text-sm font-semibold" key={key}>{label}<input className="mt-1 block w-full rounded-lg border p-2.5 dark:bg-slate-900" value={form[key]??""} onChange={e=>setForm({...form,[key]:e.target.value})}/></label>)}{[["primaryColor","Primary color"],["secondaryColor","Secondary color"]].map(([key,label])=><label className="text-sm font-semibold" key={key}>{label}<span className="mt-1 flex gap-2"><input type="color" value={form[key]} onChange={e=>setForm({...form,[key]:e.target.value})}/><input className="flex-1 rounded-lg border p-2 dark:bg-slate-900" value={form[key]} onChange={e=>setForm({...form,[key]:e.target.value})}/></span></label>)}<div className="rounded-xl p-5 text-white md:col-span-2" style={{background:`linear-gradient(135deg,${form.primaryColor},${form.secondaryColor})`}}><School/><b className="mt-3 block text-xl">{form.name}</b><span>Brand preview</span></div>{message&&<p className="text-sm md:col-span-2">{message}</p>}<button className="flex items-center justify-center gap-2 rounded-lg bg-brand-700 p-3 font-semibold text-white md:col-span-2"><Save size={17}/>Save settings</button></form></ProtectedAdminWorkspace>}
