import { ProtectedAdminWorkspace } from "../../../components/admin-workspace";
export default function SettingsPage() { return <ProtectedAdminWorkspace title="Settings" description="Configure academy preferences, roles, notifications and integrations."/>; }
