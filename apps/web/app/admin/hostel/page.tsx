"use client";
import{useCallback,useEffect,useState}from"react";import{BedDouble,Building2,Download,IndianRupee,Search,Users}from"lucide-react";import{ProtectedAdminWorkspace}from"../../../components/admin-workspace";import{errorMessage,getAccessToken}from"../../../components/auth-provider";const API=process.env.NEXT_PUBLIC_API_URL??"http://localhost:4000/api/v1";type Row=Record<string,unknown>;async function api(path:string){const response=await fetch(`${API}${path}`,{headers:{Authorization:`Bearer ${getAccessToken()}`}}),json=await response.json();if(!response.ok)throw new Error(json?.error?.message);return json}async function exportReport(name:string,format:"pdf"|"excel"){const response=await fetch(`${API}/hostel/reports/${name}?format=${format}`,{headers:{Authorization:`Bearer ${getAccessToken()}`}});if(!response.ok)throw new Error("Export failed");const url=URL.createObjectURL(await response.blob()),anchor=document.createElement("a");anchor.href=url;anchor.download=`hostel-${name}.${format==='pdf'?'pdf':'xls'}`;anchor.click();URL.revokeObjectURL(url)}export default function Page(){const[tab,setTab]=useState("hostels"),[query,setQuery]=useState(""),[rows,setRows]=useState<Row[]>([]),[dashboard,setDashboard]=useState<Row>({}),[error,setError]=useState("");const load=useCallback(async()=>{try{setError("");setDashboard((await api("/hostel/dashboard")).data);const path=tab==="hostels"?"/hostel/hostels":tab==="rooms"?"/hostel/rooms":tab==="allocations"?"/hostel/allocations":`/hostel/reports/${tab}`;setRows((await api(`${path}?search=${encodeURIComponent(query)}&page=1&limit=50`)).data)}catch(e){setError(errorMessage(e))}},[tab,query]);useEffect(()=>{void load()},[load]);const cards=[[Building2,"Hostels",dashboard.hostels],[BedDouble,"Rooms",dashboard.rooms],[Users,"Occupied",dashboard.occupied],[BedDouble,"Vacant",dashboard.vacant],[IndianRupee,"Fees due",`₹${Number(dashboard.duePaise??0)/100}`]];return <ProtectedAdminWorkspace title="Hostel Management" description="Hostels, rooms, allocations, fees, attendance, mess, visitors, assets and maintenance."><div className="mt-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-5">{cards.map(([Icon,label,value])=>{const I=Icon as typeof Building2;return <div className="card p-4" key={String(label)}><I className="text-brand-700"/><p className="mt-3 text-xs uppercase text-slate-400">{String(label)}</p><b>{String(value??0)}</b></div>})}</div><div className="mt-6 flex gap-2 overflow-x-auto">{["hostels","rooms","allocations","occupancy","vacant","fees","students","attendance","visitors","assets"].map(name=><button key={name} onClick={()=>setTab(name)} className={`rounded-full px-4 py-2 text-sm font-semibold ${tab===name?"bg-brand-700 text-white":"border bg-white dark:bg-slate-900"}`}>{name}</button>)}</div>{error&&<p className="mt-4 bg-red-50 p-3 text-red-700">{error}</p>}<div className="my-5 flex gap-3"><div className="relative flex-1"><Search className="absolute left-3 top-3" size={17}/><input value={query} onChange={e=>setQuery(e.target.value)} className="w-full rounded-lg border py-2.5 pl-9 dark:bg-slate-900" placeholder="Search hostels, rooms or students"/></div><button onClick={()=>void exportReport(tab,"excel")} className="flex items-center gap-2 rounded-lg border px-4"><Download size={17}/>Excel</button><button onClick={()=>void exportReport(tab,"pdf")} className="flex items-center gap-2 rounded-lg border px-4"><Download size={17}/>PDF</button></div><div className="card divide-y dark:divide-slate-800">{rows.map((row,index)=><div className="grid gap-2 p-4 text-sm sm:grid-cols-5" key={String(row.id??index)}>{Object.entries(row).filter(([,v])=>typeof v!=="object").slice(0,5).map(([key,value])=><span key={key}><small className="block uppercase text-slate-400">{key}</small>{String(value)}</span>)}</div>)}{!rows.length&&<p className="p-6 text-slate-500">No records found.</p>}</div><p className="mt-4 text-xs text-slate-500">Create, edit, archive, safe delete, imports, room changes, transfers, checkout, receipts, refunds, attendance, gate passes, visitors, mess, inspections, damage and maintenance workflows are available through authenticated hostel APIs.</p></ProtectedAdminWorkspace>}
