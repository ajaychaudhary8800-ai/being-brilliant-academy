"use client";
import { useEffect, useState } from "react";
import Link from "next/link";
import { BookOpen, Contrast, Download, Home, LayoutDashboard, Search } from "lucide-react";

type InstallEvent = Event & { prompt():Promise<void>; userChoice:Promise<{outcome:string}> };
export function PwaShell(){const[install,setInstall]=useState<InstallEvent|null>(null);useEffect(()=>{if("serviceWorker"in navigator)navigator.serviceWorker.register("/sw.js");const handler=(event:Event)=>{event.preventDefault();setInstall(event as InstallEvent)};window.addEventListener("beforeinstallprompt",handler);return()=>window.removeEventListener("beforeinstallprompt",handler)},[]);return <>{install&&<aside className="install-banner fixed bottom-20 left-4 right-4 z-40 mx-auto flex max-w-md items-center gap-3 rounded-2xl bg-slate-950 p-4 text-sm text-white shadow-2xl"><Download/><span className="flex-1"><b className="block">Install Being Brilliant</b>Learn faster, even offline.</span><button onClick={async()=>{await install.prompt();setInstall(null)}} className="rounded-lg bg-white px-3 py-2 font-bold text-slate-950">Install</button></aside>}<nav className="pwa-bottom fixed inset-x-0 bottom-0 z-30 grid grid-cols-5 border-t border-slate-200 bg-white/95 pb-[env(safe-area-inset-bottom)] shadow-xl backdrop-blur md:hidden" aria-label="Mobile navigation"><Nav href="/" label="Home"><Home/></Nav><Nav href="/courses" label="Courses"><Search/></Nav><Nav href="/learning-hub" label="Learn"><BookOpen/></Nav><Nav href="/dashboard" label="Dashboard"><LayoutDashboard/></Nav><button onClick={()=>document.documentElement.classList.toggle("high-contrast")} className="grid min-h-16 place-items-center text-[10px] font-bold" aria-label="Toggle high contrast"><Contrast size={20}/><span>Contrast</span></button></nav></>}
function Nav({href,label,children}:{href:string;label:string;children:React.ReactNode}){return <Link href={href} className="grid min-h-16 place-items-center text-[10px] font-bold" aria-label={label}>{children}<span>{label}</span></Link>}
