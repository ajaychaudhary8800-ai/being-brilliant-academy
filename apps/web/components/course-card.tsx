import Link from "next/link"; import { Clock, Star, Users } from "lucide-react";
export type Course = { title: string; slug: string; description: string; salePrice?: number | null; price: number; durationDays?: number | null; instructor?: { name: string } | null; _count?: { enrollments: number } };
export function CourseCard({ course }: { course: Course }) { return <article className="card overflow-hidden"><div className="h-40 bg-gradient-to-br from-brand-700 via-blue-500 to-cyan-300 p-5 text-white"><span className="pill bg-white/20 text-white">Featured programme</span><h3 className="mt-6 text-xl font-bold">{course.title}</h3></div><div className="p-5"><p className="line-clamp-2 text-sm text-slate-600 dark:text-slate-300">{course.description}</p><p className="mt-3 text-sm font-medium">{course.instructor?.name ?? "Being Brilliant Faculty"}</p><div className="mt-4 flex gap-3 text-xs text-slate-500"><span className="flex items-center gap-1"><Clock size={14}/>{course.durationDays ?? 90} days</span><span className="flex items-center gap-1"><Users size={14}/>{course._count?.enrollments ?? 0} learners</span><span className="flex items-center gap-1"><Star size={14} className="fill-amber-400 text-amber-400"/>4.9</span></div><div className="mt-5 flex items-center justify-between"><span className="text-lg font-bold">₹{((course.salePrice ?? course.price) / 100).toLocaleString("en-IN")}</span><Link href={`/courses/${course.slug}`} className="rounded-lg bg-brand-700 px-3 py-2 text-sm font-semibold text-white">Explore</Link></div></div></article>; }
