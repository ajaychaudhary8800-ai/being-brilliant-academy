import type{NextFunction,Request,Response}from"express";import jwt from"jsonwebtoken";import{Role}from"@prisma/client";import{env}from"../config.js";import{AppError}from"../lib/http.js";import{systemPrisma}from"../lib/prisma.js";import{tenantContext}from"../lib/tenant-context.js";export type AuthRequest=Request&{auth?:{userId:string;role:Role;organizationId:string;homeOrganizationId:string}};export async function requireAuth(req:AuthRequest,_res:Response,next:NextFunction){const token=req.header("authorization")?.replace(/^Bearer\s+/i,"");if(!token)return next(new AppError(401,"UNAUTHENTICATED","Sign in is required"));try{const claim=jwt.verify(token,env.JWT_ACCESS_SECRET)as{userId:string;role:Role;organizationId:string},requested=req.header("x-organization-id"),platform=claim.role===Role.SUPER_ADMIN&&claim.organizationId==="org_default",target=platform&&requested?requested:claim.organizationId,log=(allowed:boolean,reason?:string)=>systemPrisma.tenantAccessAudit.create({data:{organizationId:target,userId:claim.userId,method:req.method,path:req.originalUrl,ipAddress:req.ip,userAgent:req.header("user-agent"),allowed,reason}}).catch(()=>{});if(!claim.organizationId)throw new AppError(401,"INVALID_TOKEN","Token has no organization");if(requested&&!platform&&requested!==claim.organizationId){void log(false,"TENANT_MISMATCH");throw new AppError(403,"TENANT_MISMATCH","Cross-organization access denied")}const org=await systemPrisma.organization.findUnique({where:{id:target}}),now=new Date(),valid=!!org&&org.isActive&&!org.deletedAt&&(org.subscriptionStatus==="ACTIVE"||org.subscriptionStatus==="TRIAL"&&(!org.trialEndsAt||org.trialEndsAt>now))&&(!org.subscriptionEndsAt||org.subscriptionEndsAt>now);if(!valid){void log(false,"SUBSCRIPTION_INACTIVE");throw new AppError(402,"SUBSCRIPTION_INACTIVE","Organization subscription is inactive")}req.auth={userId:claim.userId,role:claim.role,organizationId:target,homeOrganizationId:claim.organizationId};void log(true);tenantContext.run({organizationId:target,userId:claim.userId,role:claim.role},next)}catch(error){if(error instanceof AppError)return next(error);next(new AppError(401,"INVALID_TOKEN","Your session has expired"))}}export const allow=(...roles:Role[])=>(req:AuthRequest,_res:Response,next:NextFunction)=>!req.auth||!roles.includes(req.auth.role)?next(new AppError(403,"FORBIDDEN","You do not have permission for this action")):next();
